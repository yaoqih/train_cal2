"""vNext solver package.

This package is intentionally separate from the legacy P10 runtime.  The
legacy runtime remains useful as a physical validator and audit harness, but
the vNext control flow is contract/resource/delta/episode first.
"""

